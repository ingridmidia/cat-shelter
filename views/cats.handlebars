<div>
    {{#if logged_in}}
    {{#each cats as |cat|}}
    <div class="card" style="width: 18rem;">
        <div class="card-body">
            <a href="/api/cats/{{this.id}}" class="card-title">{{cat.name}}</a>
            {{!-- <a href="/shelter/{{shelter.id}}" class="btn btn-primary">View Cats</a> --}}
        </div>
    </div>
    {{/each}}
    {{/if}}
</div>